{
  "name": "mcnote-v3",
  "version": "3.0.0",
  "description": "MCNote V3 - Conducteurs et bloc-notes",
  "scripts": {
    "build:www": "rm -rf www && mkdir -p www && cp index.html www/index.html",
    "apk": "npm run build:www && npx cap sync android && cd android && JAVA_HOME=/usr/local/opt/openjdk@21 ANDROID_HOME=/usr/local/share/android-commandlinetools ./gradlew assembleDebug && cp app/build/outputs/apk/debug/app-debug.apk ../MCNote-V3.apk",
    "apk:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/core": "^8.5.0",
    "airtable": "^0.12.2",
    "firebase-admin": "^11.11.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0"
  }
}
